/*import express from "express";
const router = express.Router();

import {addCategory} from "../controllers/settingsAdminController"



router.route("/category/addCategorie")
        .post(addCategory)

export default router;*/
